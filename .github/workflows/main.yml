name: CI/CD Workflow
on: [push] # 언제 실행할지를 정의 (예: 코드 푸시 이벤트)

jobs:
  build:
    runs-on: ubuntu-latest # 실행할 환경 (ubuntu-latest는 최신 버전의 Ubuntu)
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2 # 리포지토리 체크아웃

      # 여기에 원하는 작업을 추가
